{
  "name": "symphony-sync",
  "version": "1.0.0",
  "description": "Linear の symphony ラベル & Backlog プロジェクトを走査し、リポジトリを作成/追跡する symphony-sync 本体 (bun + TypeScript)",
  "private": true,
  "type": "module",
  "workspaces": ["linear"],
  "dependencies": {
    "linear-cli": "workspace:*"
  },
  "scripts": {
    "start": "bun run src/sync.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^7.0.2"
  }
}
